# 개발용 프론트엔드 Dockerfile
FROM node:18-alpine

WORKDIR /app

# package files 복사
COPY package*.json ./

# 의존성 설치
RUN npm install

# 모든 소스 복사 (볼륨으로 override됨)
COPY . .

# 포트 노출
EXPOSE 5173

# Vite 개발 서버 실행
CMD ["npm", "run", "dev"]